#include<locale.h>
#include<stdio.h>


int main()
{
	int dizi[5] = { 12,4,-3,71,24 };
		//*kckptr = NULL,eleman=0;
	setlocale(LC_ALL, "Turkish");
	
	kckbul(dizi, 5);
	/*#include<locale.h>
#include<stdio.h>

void kckbul(int dizi[], int index)
{
	int* kckptr = &dizi[0],eleman=0;

	for (int i = 0; i < index; i++)
	{
		if (dizi[i] < *kckptr)
		{
			kckptr = &dizi[i];
			eleman = i;
		}
	}
	printf("En küçük değer adresi %p değeri %d dir\n", kckptr, *kckptr);
	printf("En küçük değer %d nci elemandır\n", eleman);

}


int main()
{
	int dizi[20] = { 0}, *kckptr = NULL,eleman=0;
	setlocale(LC_ALL, "Turkish");
	
	kckptr = &dizi[0];
	
	
	for (int i = 0; i < 20; i++)
	{
		if (&dizi[i] < kckptr)
		{
			kckptr = &dizi[i];
			eleman = i;
		}
	}

	printf("En küçük adresli eleman adresi %p değeri %d dir\n", kckptr, *kckptr);
	printf("En küçük değer %d nci elemandır\n", eleman);

	return 0;
}

	for (int i = 0; i < 5; i++)
	{
		if (dizi[i] < *kckptr)
		{
			kckptr = &dizi[i];
			eleman = i;
		}
	}

	printf("En küçük değer adresi %p değeri %d dir\n", kckptr, *kckptr);
	printf("En küçük değer %d nci elemandır\n", eleman);
	*/
	return 0;
}
